import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // 必要に応じてスタイルを作成
const App = () => {
    return <h1>Hello, React!</h1>;
};
ReactDOM.render(<App />, document.getElementById('root'));
